@attribute [Route(AllRoutes.MyAccount)]
@attribute [Authorize]
<SeoProvider Title="حساب کاربری" />

<MudText Typo="Typo.h1" Align="Align.Center">حساب کاربری</MudText>
<MudText Class="mt-5 mb-10" Align="Align.Center">در این صفحه میتوانید اطلاعات کاربری خود را ویرایش نمایید</MudText>

@if (_CurrentUser != null)
{
    <MudCard Elevation="0">
        <MudCardContent>
            <MudGrid>
                <MudItem xs="12" md="6" lg="4">
                    <MudTextField T="string" Variant="Variant.Outlined" @bind-Value="_CurrentUser.FirstName" Label="نام" />
                </MudItem>
                <MudItem xs="12" md="6" lg="4">
                    <MudTextField T="string" Variant="Variant.Outlined" @bind-Value="_CurrentUser.LastName" Label="نام خانوادگی" />
                </MudItem>
                <MudItem xs="12" md="6" lg="4">
                    <MudTextField T="string" Variant="Variant.Outlined" @bind-Value="_CurrentUser.Company" Label="نام سازمان" />
                </MudItem>
            </MudGrid>
        </MudCardContent>
        <MudCardActions>
            <BaseButton Variant="Variant.Filled" Color="Color.Primary" OnClick="SaveInformation">ذخیره تغییرات</BaseButton>
        </MudCardActions>
    </MudCard>

    <MudCard Elevation="0" Class="mt-2">
        <MudCardContent>
            <MudGrid>
                <MudItem xs="12" md="6" lg="4">
                    <MudTextField T="string" Variant="Variant.Outlined" @bind-Value="_CurrentUser.Mobile" Label="شماره همراه" ReadOnly AdornmentIcon="@DNSLabIcons.Edit" OnAdornmentClick="EditMobile" Adornment="@(_CurrentUser.IsMobileApproved ? Adornment.None : Adornment.End)" />
                    @if (_CurrentUser.IsMobileApproved)
                    {
                        <MudText Typo="Typo.body2">شماره همراه تایید شده است.</MudText>
                    }
                    else
                    {
                        <MudText Typo="Typo.body2" Color="Color.Warning">
                            شماره همراه تایید نشده است.
                            <BaseButton Color="Color.Warning" Size="Size.Small" Variant="Variant.Outlined" OnClick="EditMobile">ارسال رمز یکبار مصرف</BaseButton>
                        </MudText>
                    }
                </MudItem>
                <MudItem xs="12" md="6" lg="4">
                    <MudTextField T="string" Variant="Variant.Outlined" @bind-Value="_CurrentUser.Email" Label="آدرس ایمیل" ReadOnly AdornmentIcon="@DNSLabIcons.Edit" OnAdornmentClick="EditEmail" Adornment="@(_CurrentUser.IsEmailApproved ? Adornment.None : Adornment.End)" />
                    @if (_CurrentUser.IsEmailApproved)
                    {
                        <MudText Typo="Typo.body2">آدرس ایمیل تایید شده است.</MudText>
                    }
                    else
                    {
                        <MudText Typo="Typo.body2" Color="Color.Warning">
                            آدرس ایمیل تایید نشده است.
                            @if (_IsConfirmEmailLinkResend)
                            {
                                <MudText Color="Color.Success" Typo="Typo.body2">لینک تایید ایمیل ارسال شد</MudText>
                                <MudText Typo="Typo.body2">لطفا پوشه Spam نیز بررسی شود</MudText>
                            }
                            else
                            {
                                <BaseButton Color="Color.Warning" Size="Size.Small" Variant="Variant.Outlined" OnClick="ResendConfirmEmailLink">ارسال مجدد لینک</BaseButton>
                            }
                        </MudText>
                    }
                </MudItem>
                <MudItem xs="12" md="6" lg="4">
                    <MudTextField T="string" Variant="Variant.Outlined" @bind-Value="_CurrentUser.Username" Label="نام کاربری" ReadOnly AdornmentIcon="@DNSLabIcons.Edit" OnAdornmentClick="EditUsername" Adornment="Adornment.End" />
                </MudItem>
                <MudItem xs="12" md="6" lg="4">
                    <MudTextField T="string" Variant="Variant.Outlined" Text="**********" Label="رمز عبور" ReadOnly AdornmentIcon="@DNSLabIcons.Edit" OnAdornmentClick="EditPassword" Adornment="Adornment.End" />
                </MudItem>
            </MudGrid>
        </MudCardContent>
    </MudCard>

    <MudDialog @bind-Visible="_EditMobileDialogVisible" Options="new DialogOptions{ MaxWidth = MaxWidth.ExtraSmall , FullWidth = true }">
        <TitleContent>
            <MudText Typo="Typo.h3">تغییر شماره همراه</MudText>
        </TitleContent>
        <DialogContent>
            <MudForm @ref="_EditMobileForm">
                <MudTextField T="string" ReadOnly="String.IsNullOrEmpty(_OtpToken)==false" Variant="Variant.Outlined" @bind-Value="_NewMobile" Label="شماره همراه" />
                @if (String.IsNullOrEmpty(_OtpToken))
                {
                    <BaseButton Variant="Variant.Filled" Class="mt-2" Color="Color.Primary" FullWidth OnClick="SaveNewMobile">ذخیره شماره همراه جدید</BaseButton>
                }
                else
                {
                    <MudText Typo="Typo.caption">یک کد یکبار مصرف برای شما ارسال شد لطفا در کادر زیر وارد نمایید</MudText>
                    <MudTextField T="string" Variant="Variant.Outlined" @bind-Value="_Otp" Label="کد احراز هویت" />
                    <Timer ResendOtpCallBack="ResendOtp" />
                    <BaseButton Variant="Variant.Filled" Class="mt-2" Color="Color.Primary" FullWidth OnClick="ConfirmOtp">تایید</BaseButton>
                }
            </MudForm>
        </DialogContent>
    </MudDialog>

    <MudDialog @bind-Visible="_EditUsernameDialogVisible" Options="new DialogOptions{ MaxWidth = MaxWidth.ExtraSmall , FullWidth = true }">
        <TitleContent>
            <MudText Typo="Typo.h3">تغییر نام کاربری</MudText>
        </TitleContent>
        <DialogContent>
            <MudTextField T="string" Variant="Variant.Outlined" @bind-Value="_NewUsername" Label="نام کاربری" />
            <BaseButton Variant="Variant.Filled" Class="mt-2" Color="Color.Primary" FullWidth OnClick="SaveNewUsername">ذخیره نام کاربری جدید</BaseButton>
        </DialogContent>
    </MudDialog>

    <MudDialog @bind-Visible="_EditEmailDialogVisible" Options="new DialogOptions{ MaxWidth = MaxWidth.ExtraSmall , FullWidth = true }">
        <TitleContent>
            <MudText Typo="Typo.h3">تغییر آدرس ایمیل</MudText>
        </TitleContent>
        <DialogContent>
            <MudForm @ref="_EditEmailForm">
                <MudTextField T="string" Variant="Variant.Outlined" Required RequiredError="آدرس ایمیل را وارد نمایید" @bind-Value="_NewEmailAddress" Label="آدرس ایمیل" />
                <BaseButton Variant="Variant.Filled" Class="mt-2" Color="Color.Primary" FullWidth OnClick="SaveNewEmail">ذخیره آدرس ایمیل جدید</BaseButton>
            </MudForm>
        </DialogContent>
    </MudDialog>

    <MudDialog @bind-Visible="_EditPasswordDialogVisible" Options="new DialogOptions{ MaxWidth = MaxWidth.ExtraSmall , FullWidth = true }">
        <TitleContent>
            <MudText Typo="Typo.h3">تغییر رمز عبور</MudText>
        </TitleContent>
        <DialogContent>
            <MudForm @ref="_EditPasswordForm">
                <MudText Typo="Typo.caption" Color="Color.Info">در صورتی که تا به حال رمز عبور تنظیم نکرده‌ اید رمز عبور جاری را وارد ننمایید</MudText>
                <MudTextField T="string" InputType="InputType.Password" Variant="Variant.Outlined" @bind-Value="_ChangePasswordDTO.CurrentPassword" Label="رمز عبور جاری" />
                <MudTextField T="string" InputType="InputType.Password" Variant="Variant.Outlined" Required RequiredError="رمز عبور جدید را وارد نمایید" @bind-Value="_ChangePasswordDTO.NewPassword" Label="رمز عبور جدید" />
                <BaseButton Variant="Variant.Filled" Class="mt-2" Color="Color.Primary" FullWidth OnClick="SaveNewPassword">ذخیره رمز عبور جدید</BaseButton>
            </MudForm>
        </DialogContent>
    </MudDialog>
}
