@attribute [Route(AllRoutes.CallBackPayment)]
@attribute [Authorize]
<SeoProvider Title="نتیجه پرداخت" />

<MudCard Elevation="0">
    <MudCardHeader>
        <CardHeaderActions>
            <MudButton EndIcon="@DNSLabIcons.Back" Variant="Variant.Filled" Color="Color.Default" Size="MudBlazor.Size.Small" Href="@AllRoutes.MyWallet">بازگشت</MudButton>
        </CardHeaderActions>
    </MudCardHeader>
    <MudCardContent>
        @if (IsVerified == null)
        {
            <MudStack AlignItems="AlignItems.Center">
                <MudProgressCircular Color="Color.Primary" Indeterminate />
            </MudStack>
        }
        else
        {
            <MudAlert ContentAlignment="HorizontalAlignment.Start" Severity="@(IsVerified == true ? Severity.Success : Severity.Warning)">
                <MudText>@(IsVerified == true ? "موفق" : "نا موفق")</MudText>
                <MudText Typo="Typo.body2">توضیحات : @Result</MudText>
            </MudAlert>
        }
    </MudCardContent>
</MudCard>